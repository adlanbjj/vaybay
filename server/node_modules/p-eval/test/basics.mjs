#!/usr/bin/env nodemjs
// -*- coding: utf-8, tab-width: 2 -*-

import test from 'p-tape';

function err2str(p) { p.then(() => false, String); }

test('basics', async (t) => {
  t.plan(4);

  await


});
